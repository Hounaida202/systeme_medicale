<?xml version="1.0" encoding="UTF-8" ?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence" version="3.0">
    <persistence-unit name="systeme_medicale" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <class>com.systeme_medicale.Entities.Patient</class>
        <class>com.systeme_medicale.Entities.User</class>
        <class>com.systeme_medicale.Entities.Signe</class>

        <properties>
            <!-- Connexion à la base -->
            <property name="jakarta.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:postgresql://localhost:5432/systeme_medicale"/>
            <property name="jakarta.persistence.jdbc.user" value="postgres"/>
            <property name="jakarta.persistence.jdbc.password" value="etrichi"/>

            <!-- Dialecte Hibernate pour PostgreSQL -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>

            <!-- Création automatique des tables -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>

            <!-- Pour voir les requêtes SQL -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>
